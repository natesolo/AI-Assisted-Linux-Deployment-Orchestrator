services:
  postgres:
    image: postgres:16
    container_name: aidlo-postgres
    environment:
      POSTGRES_DB: aidlo
      POSTGRES_USER: aidlo
      POSTGRES_PASSWORD: aidlo
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
